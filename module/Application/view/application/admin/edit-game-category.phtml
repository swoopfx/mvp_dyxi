<div class="container mt-5">
    <h2>Edit Game Category</h2>
    <?php if ($this->error): ?>
        <div class="alert alert-danger"><?= $this->error ?></div>
    <?php endif; ?>
    <?php if ($this->success): ?>
        <div class="alert alert-success"><?= $this->success ?></div>
    <?php endif; ?>

    <form method="post" class="mt-4">
        <div class="mb-3">
            <label class="form-label">Category Name</label>
            <input type="text" name="gameCategory" class="form-control" value="<?= $this->escapeHtml($this->category->getGameCategory()) ?>" required>
        </div>
        <button type="submit" class="btn btn-primary">Update Category</button>
        <a href="<?= $this->url('admin', ['action' => 'view-game-categories']) ?>" class="btn btn-secondary">Back to List</a>
    </form>
</div>
