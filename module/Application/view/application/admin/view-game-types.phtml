<div class="container mt-5">
    <h2>View Game Types</h2>
    <div class="table-responsive mt-4">
        <table class="table table-striped table-hover">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Type Name</th>
                    <th>Options</th>
                </tr>
            </thead>
            <tbody>
                <?php if (count($this->types) > 0): ?>
                    <?php foreach ($this->types as $type): ?>
                    <tr>
                        <td><?= $type->getId() ?></td>
                        <td><?= $this->escapeHtml($type->getType()) ?></td>
                        <td>
                            <a href="<?= $this->url('admin', ['action' => 'edit-game-type']) ?>?id=<?= $type->getId() ?>" class="btn btn-sm btn-primary">Edit</a>
                        </td>
                    </tr>
                    <?php endforeach; ?>
                <?php else: ?>
                    <tr><td colspan="3" class="text-center">No game types found.</td></tr>
                <?php endif; ?>
            </tbody>
        </table>
    </div>
    <a href="<?= $this->url('admin') ?>" class="btn btn-secondary">Back to Dashboard</a>
</div>
