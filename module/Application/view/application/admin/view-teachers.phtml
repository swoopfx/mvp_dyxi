<div class="container mt-5">
    <div class="row">
        <div class="col-12">
            <div class="card border-0 shadow-lg" style="border-radius: 16px; overflow: hidden; background: rgba(255, 255, 255, 0.95); backdrop-filter: blur(10px);">
                <div class="card-header bg-gradient-primary text-white p-4" style="background: linear-gradient(135deg, #0d9488 0%, #0891b2 100%);">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h3 class="mb-1 text-white font-weight-bold">Registered Teachers</h3>
                            <p class="mb-0 text-white-50 small">Manage teacher registration credentials, ID codes, and join logs.</p>
                        </div>
                        <i class="bi bi-person-badge-fill fs-2 text-white-50"></i>
                    </div>
                </div>
                <div class="card-body p-4">
                    <!-- Quick Search bar -->
                    <div class="row mb-4 justify-content-end align-items-center">
                        <div class="col-md-5">
                            <div class="input-group shadow-sm" style="border-radius: 8px; overflow: hidden;">
                                <span class="input-group-text bg-white border-0"><i class="bi bi-search text-muted"></i></span>
                                <input type="text" id="teacherSearch" class="form-control border-0" placeholder="Filter by teacher name, ID code..." style="padding: 10px 15px; outline: none; box-shadow: none;">
                            </div>
                        </div>
                    </div>

                    <div class="table-responsive" style="border-radius: 12px; border: 1px solid #e2e8f0; overflow: hidden;">
                        <table class="table table-hover align-middle mb-0" id="teacherTable">
                            <thead class="bg-light text-muted uppercase font-weight-semibold small">
                                <tr>
                                    <th class="ps-4">Database ID</th>
                                    <th>Teacher Details</th>
                                    <th>Teacher ID Code</th>
                                    <th class="pe-4">Registration Date</th>
                                </tr>
                            </thead>
                            <tbody>
                                <?php if (count($this->teachers ?? []) > 0): ?>
                                    <?php foreach ($this->teachers as $teacher): ?>
                                        <tr>
                                            <td class="ps-4 text-muted small">#<?= $teacher->getId() ?></td>
                                            <td>
                                                <div class="d-flex align-items-center">
                                                    <div class="avatar-circle me-3 d-flex align-items-center justify-content-center text-white bg-primary font-weight-semibold shadow-sm" style="width: 38px; height: 38px; border-radius: 50%; background: linear-gradient(135deg, #0d9488 0%, #0891b2 100%);">
                                                        <?= strtoupper(substr($teacher->getTeacherName() ?: 'T', 0, 1)) ?>
                                                    </div>
                                                    <div>
                                                        <span class="font-weight-semibold d-block" style="color: #1e293b;"><?= $this->escapeHtml($teacher->getTeacherName()) ?></span>
                                                        <span class="text-muted small">Verified Instructor</span>
                                                    </div>
                                                </div>
                                            </td>
                                            <td>
                                                <code class="px-2 py-1 bg-light text-secondary rounded small"><?= $this->escapeHtml($teacher->getTeacherId()) ?></code>
                                            </td>
                                            <td class="pe-4 text-muted">
                                                <i class="bi bi-clock me-1 text-muted-400"></i>
                                                <?= $teacher->getCreatedAt() ? $teacher->getCreatedAt()->format('F d, Y \a\t H:i') : 'N/A' ?>
                                            </td>
                                        </tr>
                                    <?php endforeach; ?>
                                <?php else: ?>
                                    <tr>
                                        <td colspan="4" class="text-center py-5 text-muted">
                                            <i class="bi bi-people fs-1 d-block mb-3 text-muted-300"></i>
                                            No teachers registered in the database.
                                        </td>
                                    </tr>
                                <?php endif; ?>
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="card-footer bg-light p-3 d-flex justify-content-between align-items-center">
                    <a href="<?= $this->url('admin') ?>" class="btn btn-outline-secondary px-4 shadow-sm" style="border-radius: 8px;">
                        <i class="bi bi-arrow-left me-2"></i> Admin Panel
                    </a>
                    <span class="text-muted small">Total: <?= count($this->teachers ?? []) ?> teachers registered.</span>
                </div>
            </div>
        </div>
    </div>
</div>

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

<script>
document.addEventListener('DOMContentLoaded', function() {
    const searchInput = document.getElementById('teacherSearch');
    if (searchInput) {
        searchInput.addEventListener('keyup', function() {
            const query = this.value.toLowerCase();
            const rows = document.querySelectorAll('#teacherTable tbody tr');
            
            rows.forEach(row => {
                const text = row.textContent.toLowerCase();
                if (text.includes(query)) {
                    row.style.display = '';
                } else {
                    row.style.display = 'none';
                }
            });
        });
    }
});
</script>
