<div class="container mt-5">
    <h2>Edit Game Language</h2>
    <?php if ($this->error): ?>
        <div class="alert alert-danger"><?= $this->error ?></div>
    <?php endif; ?>
    <?php if ($this->success): ?>
        <div class="alert alert-success"><?= $this->success ?></div>
    <?php endif; ?>

    <form method="post" class="mt-4">
        <div class="mb-3">
            <label class="form-label">Language Name</label>
            <input type="text" name="language" class="form-control" value="<?= $this->escapeHtml($this->language->getLanguage()) ?>" required>
        </div>
        <button type="submit" class="btn btn-primary">Update Language</button>
        <a href="<?= $this->url('admin', ['action' => 'view-game-languages']) ?>" class="btn btn-secondary">Back to List</a>
    </form>
</div>
