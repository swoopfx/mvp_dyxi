<?php
$title = 'Create Game';
$this->headTitle($title);
?>
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-lg-10">
            <div class="card border-0 shadow-lg" style="border-radius: 16px; overflow: hidden; background: rgba(255, 255, 255, 0.95); backdrop-filter: blur(10px);">
                <div class="card-header bg-gradient-primary text-white p-4" style="background: linear-gradient(135deg, #4f46e5 0%, #3b82f6 100%);">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h3 class="mb-1 text-white font-weight-bold"><?= $this->escapeHtml($title) ?></h3>
                            <p class="mb-0 text-white-50 small">Configure and register a new game in the system</p>
                        </div>
                        <i class="bi bi-controller fs-2 text-white-50"></i>
                    </div>
                </div>
                <div class="card-body p-5">
                    <?php if ($this->error): ?>
                        <div class="alert alert-danger d-flex align-items-center border-0 shadow-sm mb-4" role="alert" style="border-radius: 12px; background-color: #fee2e2; color: #b91c1c;">
                            <i class="bi bi-exclamation-triangle-fill me-2 fs-5"></i>
                            <div><?= $this->escapeHtml($this->error) ?></div>
                        </div>
                    <?php endif; ?>

                    <?php if ($this->success): ?>
                        <div class="alert alert-success d-flex align-items-center border-0 shadow-sm mb-4" role="alert" style="border-radius: 12px; background-color: #d1fae5; color: #047857;">
                            <i class="bi bi-check-circle-fill me-2 fs-5"></i>
                            <div><?= $this->escapeHtml($this->success) ?></div>
                        </div>
                    <?php endif; ?>

                    <form method="post" action="<?= $this->url('admin', ['action' => 'create-game']) ?>" class="needs-validation">
                        <div class="row g-4">
                            <!-- Game Name -->
                            <div class="col-md-6">
                                <label class="form-label font-weight-semibold text-muted small uppercase">Game Name</label>
                                <div class="input-group shadow-sm" style="border-radius: 8px; overflow: hidden;">
                                    <span class="input-group-text bg-light border-0"><i class="bi bi-gamepad text-muted"></i></span>
                                    <input type="text" class="form-control bg-light border-0" id="gameName" name="gameName" placeholder="Enter Game Name" required style="padding: 10px 15px;">
                                </div>
                            </div>

                            <!-- Game Page -->
                            <div class="col-md-6">
                                <label class="form-label font-weight-semibold text-muted small uppercase">Game Page Reference</label>
                                <div class="input-group shadow-sm" style="border-radius: 8px; overflow: hidden;">
                                    <span class="input-group-text bg-light border-0"><i class="bi bi-link-45deg text-muted"></i></span>
                                    <input type="text" class="form-control bg-light border-0" id="gamePage" name="gamePage" placeholder="e.g. QML page reference" required style="padding: 10px 15px;">
                                </div>
                            </div>

                            <!-- Game Age Bracket -->
                            <div class="col-md-6">
                                <label class="form-label font-weight-semibold text-muted small uppercase">Game Bracket</label>
                                <div class="input-group shadow-sm" style="border-radius: 8px; overflow: hidden;">
                                    <span class="input-group-text bg-light border-0"><i class="bi bi-calendar-range text-muted"></i></span>
                                    <select class="form-select bg-light border-0" id="gameAgeBracket" name="gameAgeBracket" required style="padding: 10px 15px;">
                                        <option value="">-- Select Bracket --</option>
                                        <?php foreach ($this->gameAgeBrackets as $bracket): ?>
                                            <option value="<?= $this->escapeHtmlAttr($bracket->getId()) ?>">
                                                <?= $this->escapeHtml($bracket->getAgeBracket() ?: 'ID: ' . $bracket->getId()) ?>
                                            </option>
                                        <?php endforeach; ?>
                                    </select>
                                </div>
                            </div>

                            <!-- Language -->
                            <div class="col-md-6">
                                <label class="form-label font-weight-semibold text-muted small uppercase">Language</label>
                                <div class="input-group shadow-sm" style="border-radius: 8px; overflow: hidden;">
                                    <span class="input-group-text bg-light border-0"><i class="bi bi-translate text-muted"></i></span>
                                    <select class="form-select bg-light border-0" id="language" name="language" required style="padding: 10px 15px;">
                                        <option value="">-- Select Language --</option>
                                        <?php foreach ($this->gameLanguages as $lang): ?>
                                            <option value="<?= $this->escapeHtmlAttr($lang->getId()) ?>">
                                                <?= $this->escapeHtml($lang->getLanguage() ?: 'ID: ' . $lang->getId()) ?>
                                            </option>
                                        <?php endforeach; ?>
                                    </select>
                                </div>
                            </div>

                            <!-- Graphic Multiple Select for Game Type -->
                            <div class="col-12 mt-4">
                                <label class="form-label font-weight-semibold text-muted small uppercase d-block mb-3">Game Type (Select all that apply)</label>
                                <div class="row g-3">
                                    <?php foreach ($this->gameTypes as $type): ?>
                                        <div class="col-md-4">
                                            <input type="checkbox" class="btn-check" id="type_<?= $type->getId() ?>" name="gameType[]" value="<?= $this->escapeHtmlAttr($type->getId()) ?>">
                                            <label class="btn btn-outline-primary w-100 text-start p-3 d-flex align-items-center justify-content-between option-card shadow-sm" for="type_<?= $type->getId() ?>">
                                                <div class="d-flex align-items-center">
                                                    <i class="bi bi-controller me-2 text-muted card-icon fs-5"></i>
                                                    <span class="font-weight-semibold"><?= $this->escapeHtml($type->getType() ?: 'ID: ' . $type->getId()) ?></span>
                                                </div>
                                                <i class="bi bi-check-circle-fill text-primary check-icon fs-5"></i>
                                            </label>
                                        </div>
                                    <?php endforeach; ?>
                                </div>
                            </div>

                            <!-- Graphic Multiple Select for Game Category -->
                            <div class="col-12 mt-4">
                                <label class="form-label font-weight-semibold text-muted small uppercase d-block mb-3">Game Category (Select all that apply)</label>
                                <div class="row g-3">
                                    <?php foreach ($this->gameCategories as $category): ?>
                                        <div class="col-md-4">
                                            <input type="checkbox" class="btn-check" id="cat_<?= $category->getId() ?>" name="gameCategory[]" value="<?= $this->escapeHtmlAttr($category->getId()) ?>">
                                            <label class="btn btn-outline-primary w-100 text-start p-3 d-flex align-items-center justify-content-between option-card shadow-sm" for="cat_<?= $category->getId() ?>">
                                                <div class="d-flex align-items-center">
                                                    <i class="bi bi-bookmark-star me-2 text-muted card-icon fs-5"></i>
                                                    <span class="font-weight-semibold"><?= $this->escapeHtml($category->getGameCategory() ?: 'ID: ' . $category->getId()) ?></span>
                                                </div>
                                                <i class="bi bi-check-circle-fill text-primary check-icon fs-5"></i>
                                            </label>
                                        </div>
                                    <?php endforeach; ?>
                                </div>
                            </div>

                            <!-- Game Definition -->
                            <div class="col-12 mt-4">
                                <label class="form-label font-weight-semibold text-muted small uppercase">Game Definition (Description or JSON)</label>
                                <textarea class="form-control bg-light border-0 shadow-sm" id="gameDefinition" name="gameDefinition" rows="4" placeholder="Enter Description or JSON definition" required style="border-radius: 8px; padding: 12px 15px;"></textarea>
                            </div>
                        </div>

                        <!-- Action Buttons -->
                        <div class="mt-5 d-flex justify-content-between align-items-center">
                            <a href="<?= $this->url('admin') ?>" class="btn btn-light px-4 py-2 font-weight-semibold shadow-sm" style="border-radius: 10px;">
                                <i class="bi bi-arrow-left me-2"></i> Back to Dashboard
                            </a>
                            <button type="submit" class="btn btn-primary px-5 py-2 font-weight-semibold shadow-sm" style="background: linear-gradient(135deg, #4f46e5 0%, #3b82f6 100%); border: none; border-radius: 10px;">
                                <i class="bi bi-plus-circle me-2"></i> Create Game
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.option-card {
    border: 2px solid #e2e8f0 !important;
    background-color: #ffffff !important;
    color: #475569 !important;
    border-radius: 12px !important;
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1) !important;
    cursor: pointer;
}

.option-card:hover {
    border-color: #cbd5e1 !important;
    background-color: #f8fafc !important;
    transform: translateY(-2px);
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -1px rgba(0, 0, 0, 0.03) !important;
}

.btn-check:checked + .option-card {
    border-color: #3b82f6 !important;
    background-color: #f0f7ff !important;
    color: #1e3a8a !important;
    box-shadow: 0 10px 15px -3px rgba(59, 130, 246, 0.1), 0 4px 6px -4px rgba(59, 130, 246, 0.1) !important;
}

.btn-check:checked + .option-card .card-icon {
    color: #3b82f6 !important;
}

.option-card .check-icon {
    opacity: 0;
    transform: scale(0.7);
    transition: all 0.2s cubic-bezier(0.34, 1.56, 0.64, 1);
}

.btn-check:checked + .option-card .check-icon {
    opacity: 1;
    transform: scale(1);
}
</style>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

<script>
document.addEventListener('DOMContentLoaded', function() {
    const gameNameInput = document.getElementById('gameName');
    const gamePageInput = document.getElementById('gamePage');

    if (gameNameInput && gamePageInput) {
        gameNameInput.addEventListener('input', function() {
            // Replace spaces/whitespace with underscores in real-time
            gamePageInput.value = this.value.replace(/\s+/g, '_');
        });
    }
});
</script>
