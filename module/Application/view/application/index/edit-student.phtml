<div class="container mt-5">
    <?php if ($this->student): ?>
        <!-- Edit Student Form -->
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="card border-0 shadow-lg" style="border-radius: 16px; overflow: hidden; background: rgba(255, 255, 255, 0.95); backdrop-filter: blur(10px);">
                    <div class="card-header bg-gradient-primary text-white p-4" style="background: linear-gradient(135deg, #4f46e5 0%, #3b82f6 100%);">
                        <div class="d-flex justify-content-between align-items-center">
                            <div>
                                <h3 class="mb-1 text-white font-weight-bold">Edit Student Details</h3>
                                <p class="mb-0 text-white-50 small">Modify the profiles and credentials of student <?= $this->escapeHtml($this->student->getStudentName()) ?></p>
                            </div>
                            <span class="badge bg-light text-primary px-3 py-2 fs-6 font-weight-semibold" style="border-radius: 20px;"><?= $this->escapeHtml($this->student->getStudentId()) ?></span>
                        </div>
                    </div>
                    <div class="card-body p-5">
                        <?php if ($this->error): ?>
                            <div class="alert alert-danger d-flex align-items-center border-0 shadow-sm mb-4" role="alert" style="border-radius: 12px; background-color: #fee2e2; color: #b91c1c;">
                                <i class="bi bi-exclamation-triangle-fill me-2 fs-5"></i>
                                <div><?= $this->error ?></div>
                            </div>
                        <?php endif; ?>
                        <?php if ($this->success): ?>
                            <div class="alert alert-success d-flex align-items-center border-0 shadow-sm mb-4" role="alert" style="border-radius: 12px; background-color: #d1fae5; color: #047857;">
                                <i class="bi bi-check-circle-fill me-2 fs-5"></i>
                                <div><?= $this->success ?></div>
                            </div>
                        <?php endif; ?>

                        <form method="post" class="needs-validation">
                            <div class="row g-4">
                                <div class="col-md-6">
                                    <label class="form-label font-weight-semibold text-muted small uppercase">Student Name</label>
                                    <div class="input-group">
                                        <span class="input-group-text bg-light border-0"><i class="bi bi-person text-muted"></i></span>
                                        <input type="text" name="studentName" class="form-control bg-light border-0" value="<?= $this->escapeHtml($this->student->getStudentName()) ?>" placeholder="Enter Full Name" required style="border-radius: 0 8px 8px 0; padding: 10px 15px;">
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label font-weight-semibold text-muted small uppercase">Student ID Code</label>
                                    <div class="input-group">
                                        <span class="input-group-text bg-light border-0"><i class="bi bi-card-text text-muted"></i></span>
                                        <input type="text" name="studentId" class="form-control bg-light border-0" value="<?= $this->escapeHtml($this->student->getStudentId()) ?>" placeholder="e.g. S1" required style="border-radius: 0 8px 8px 0; padding: 10px 15px;">
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label font-weight-semibold text-muted small uppercase">Age Bracket</label>
                                    <div class="input-group">
                                        <span class="input-group-text bg-light border-0"><i class="bi bi-calendar-range text-muted"></i></span>
                                        <select name="studentAge" class="form-select bg-light border-0" required style="border-radius: 0 8px 8px 0; padding: 10px 15px;">
                                            <option value="">Select Age Bracket</option>
                                            <?php $currentAgeId = $this->student->getStudentAge() ? $this->student->getStudentAge()->getId() : ''; ?>
                                            <?php foreach ($this->brackets ?? [] as $bracket): ?>
                                                <option value="<?= $bracket->getId() ?>" <?= (string)$currentAgeId === (string)$bracket->getId() ? 'selected' : '' ?>>
                                                    <?= $this->escapeHtml($bracket->getAgeBracket()) ?>
                                                </option>
                                            <?php endforeach; ?>
                                        </select>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label font-weight-semibold text-muted small uppercase">Language</label>
                                    <div class="input-group">
                                        <span class="input-group-text bg-light border-0"><i class="bi bi-translate text-muted"></i></span>
                                        <select name="language" class="form-select bg-light border-0" required style="border-radius: 0 8px 8px 0; padding: 10px 15px;">
                                            <option value="">Select Language</option>
                                            <?php $currentLangId = $this->student->getLanguage() ? $this->student->getLanguage()->getId() : ''; ?>
                                            <?php foreach ($this->languages ?? [] as $lang): ?>
                                                <option value="<?= $lang->getId() ?>" <?= (string)$currentLangId === (string)$lang->getId() ? 'selected' : '' ?>>
                                                    <?= $this->escapeHtml($lang->getLanguage()) ?>
                                                </option>
                                            <?php endforeach; ?>
                                        </select>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label font-weight-semibold text-muted small uppercase">Physical Age (Numeric)</label>
                                    <div class="input-group">
                                        <span class="input-group-text bg-light border-0"><i class="bi bi-123 text-muted"></i></span>
                                        <input type="number" name="age" min="1" max="100" class="form-control bg-light border-0" value="<?= $this->student->getAge() ? $this->escapeHtml($this->student->getAge()) : '' ?>" placeholder="Enter Age" style="border-radius: 0 8px 8px 0; padding: 10px 15px;">
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label font-weight-semibold text-muted small uppercase">Teacher ID Code</label>
                                    <div class="input-group">
                                        <span class="input-group-text bg-light border-0"><i class="bi bi-person-badge text-muted"></i></span>
                                        <input type="text" name="teacherid" class="form-control bg-light border-0" value="<?= $this->student->getTeacherId() ? $this->escapeHtml($this->student->getTeacherId()->getTeacherId()) : '' ?>" placeholder="e.g. T1" style="border-radius: 0 8px 8px 0; padding: 10px 15px;">
                                    </div>
                                </div>
                                <div class="col-12 mt-4">
                                    <div class="form-check form-switch p-3 border rounded-3 bg-light d-flex align-items-center justify-content-between" style="border-radius: 12px;">
                                        <div class="me-3">
                                            <label class="form-check-label font-weight-semibold" for="dyslexicCheck" style="font-size: 1rem; color: #1e293b;">Child Is Possibly Dyslexic</label>
                                            <div class="text-muted small">Toggle this switch if the student shows signs of dyslexia.</div>
                                        </div>
                                        <input type="checkbox" name="isDyslexic" class="form-check-input" id="dyslexicCheck" style="width: 2.8em; height: 1.4em; cursor: pointer;" <?= $this->student->getIsDyslexic() ? 'checked' : '' ?>>
                                    </div>
                                </div>
                            </div>

                            <div class="mt-5 d-flex justify-content-between align-items-center">
                                <a href="<?= $this->url('admin', ['action' => 'view-student']) ?>" class="btn btn-light px-4 py-2 font-weight-semibold shadow-sm" style="border-radius: 10px;">
                                    <i class="bi bi-arrow-left me-2"></i> Back to Dashboard
                                </a>
                                <button type="submit" class="btn btn-primary px-5 py-2 font-weight-semibold shadow-sm" style="background: linear-gradient(135deg, #4f46e5 0%, #3b82f6 100%); border: none; border-radius: 10px;">
                                    <i class="bi bi-save me-2"></i> Update Student Profile
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>

    <?php else: ?>
        <!-- Student Selection List (No student ID in query) -->
        <div class="row">
            <div class="col-12">
                <div class="card border-0 shadow-lg" style="border-radius: 16px; overflow: hidden; background: rgba(255, 255, 255, 0.95); backdrop-filter: blur(10px);">
                    <div class="card-header bg-gradient-primary text-white p-4" style="background: linear-gradient(135deg, #0ea5e9 0%, #2563eb 100%);">
                        <div class="d-flex justify-content-between align-items-center">
                            <div>
                                <h3 class="mb-1 text-white font-weight-bold">Select Student to Edit</h3>
                                <p class="mb-0 text-white-50 small">Search or choose a student from the register list below to edit details.</p>
                            </div>
                            <i class="bi bi-person-workspace fs-2 text-white-50"></i>
                        </div>
                    </div>
                    <div class="card-body p-4">
                        <!-- Quick Search bar -->
                        <div class="row mb-4 justify-content-end">
                            <div class="col-md-5">
                                <div class="input-group shadow-sm" style="border-radius: 8px; overflow: hidden;">
                                    <span class="input-group-text bg-white border-0"><i class="bi bi-search text-muted"></i></span>
                                    <input type="text" id="studentSearch" class="form-control border-0" placeholder="Filter by name, ID or age..." style="padding: 10px 15px; outline: none; box-shadow: none;">
                                </div>
                            </div>
                        </div>

                        <div class="table-responsive" style="border-radius: 12px; border: 1px solid #e2e8f0; overflow: hidden;">
                            <table class="table table-hover align-middle mb-0" id="studentsTable">
                                <thead class="bg-light text-muted uppercase font-weight-semibold small">
                                    <tr>
                                        <th class="ps-4">Database ID</th>
                                        <th>Student Name</th>
                                        <th>Student ID Code</th>
                                        <th>Age Bracket</th>
                                        <th>Language</th>
                                        <th>Dyslexic?</th>
                                        <th class="text-end pe-4">Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <?php if (count($this->students ?? []) > 0): ?>
                                        <?php foreach ($this->students as $student): ?>
                                            <tr>
                                                <td class="ps-4 text-muted small">#<?= $student->getId() ?></td>
                                                <td>
                                                    <div class="d-flex align-items-center">
                                                        <div class="avatar-circle me-3 d-flex align-items-center justify-content-center text-white bg-primary font-weight-semibold" style="width: 38px; height: 38px; border-radius: 50%; background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);">
                                                            <?= strtoupper(substr($student->getStudentName(), 0, 1)) ?>
                                                        </div>
                                                        <span class="font-weight-semibold" style="color: #1e293b;"><?= $this->escapeHtml($student->getStudentName()) ?></span>
                                                    </div>
                                                </td>
                                                <td>
                                                    <span class="badge bg-soft-primary text-primary px-3 py-1.5 font-weight-semibold" style="background-color: #eff6ff; color: #2563eb; border-radius: 6px;"><?= $this->escapeHtml($student->getStudentId()) ?></span>
                                                </td>
                                                <td>
                                                    <?= $student->getStudentAge() ? $this->escapeHtml($student->getStudentAge()->getAgeBracket()) : '<span class="text-muted small">Not set</span>' ?>
                                                </td>
                                                <td>
                                                    <?= $student->getLanguage() ? $this->escapeHtml($student->getLanguage()->getLanguage()) : '<span class="text-muted small">Not set</span>' ?>
                                                </td>
                                                <td>
                                                    <?php if ($student->getIsDyslexic()): ?>
                                                        <span class="badge bg-soft-danger px-3 py-1.5 text-danger font-weight-semibold" style="background-color: #fef2f2; color: #dc2626; border-radius: 6px;">Yes</span>
                                                    <?php else: ?>
                                                        <span class="badge bg-soft-success px-3 py-1.5 text-success font-weight-semibold" style="background-color: #f0fdf4; color: #16a34a; border-radius: 6px;">No</span>
                                                    <?php endif; ?>
                                                </td>
                                                <td class="text-end pe-4">
                                                    <a href="<?= $this->url('application', ['action' => 'edit-student']) ?>?id=<?= $student->getId() ?>" class="btn btn-sm btn-primary px-3 shadow-sm" style="border-radius: 6px; background-color: #2563eb; border: none;">
                                                        <i class="bi bi-pencil me-1"></i> Edit
                                                    </a>
                                                </td>
                                            </tr>
                                        <?php endforeach; ?>
                                    <?php else: ?>
                                        <tr>
                                            <td colspan="7" class="text-center py-5 text-muted">
                                                <i class="bi bi-inbox fs-1 d-block mb-3 text-muted-300"></i>
                                                No students found in the database.
                                            </td>
                                        </tr>
                                    <?php endif; ?>
                                </tbody>
                            </table>
                        </div>
                    </div>
                    <div class="card-footer bg-light p-3 d-flex justify-content-between align-items-center">
                        <a href="<?= $this->url('admin') ?>" class="btn btn-outline-secondary px-4 shadow-sm" style="border-radius: 8px;">
                            <i class="bi bi-arrow-left me-2"></i> Admin Panel
                        </a>
                        <span class="text-muted small">Total: <?= count($this->students ?? []) ?> students registered.</span>
                    </div>
                </div>
            </div>
        </div>
    <?php endif; ?>
</div>

<!-- Icons style inclusion just in case -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

<!-- Client-side filter for student list -->
<script>
document.addEventListener('DOMContentLoaded', function() {
    const searchInput = document.getElementById('studentSearch');
    if (searchInput) {
        searchInput.addEventListener('keyup', function() {
            const query = this.value.toLowerCase();
            const rows = document.querySelectorAll('#studentsTable tbody tr');
            
            rows.forEach(row => {
                const text = row.textContent.toLowerCase();
                if (text.includes(query)) {
                    row.style.display = '';
                } else {
                    row.style.display = 'none';
                }
            });
        });
    }
});
</script>
