services:
  laminas:
    build: .
    ports:
     - "8080:80"
    volumes:
     - .:/var/www
    depends_on:
      - db

  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root 
      MYSQL_DATABASE: mvp_dyxi
      MYSQL_USER: ezekiel
      MYSQL_PASSWORD: Oluwaseun1@

